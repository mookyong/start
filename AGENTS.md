# AGENTS.md

## 목적
- 이 문서는 `[프로젝트명]`에서 작업하는 에이전트의 운영 규칙 템플릿이다.
- 실제 프로젝트에 맞게 항목을 채워서 사용한다.

## 적용 범위
- 이 문서는 저장소 전체에 적용한다.
- 하위 규칙 파일이 있으면 더 구체적인 지침을 우선한다.

## 기록 규칙
- 사용자 요청, 진행한 작업, 확인 결과, 막힌 지점만 간단히 기록한다.
- `AGENT_BACKLOG.md`가 있으면 턴 단위로 누적 기록한다.
- `AGENT_BACKLOG.md`는 100턴을 넘기면 별도 파일로 분리하는 것을 검토한다.
- 분리 시에는 인덱스 문서를 함께 둔다.
- 중요한 결정은 `DECISIONS.md`에 별도로 기록한다.
- 세부 추론, 장황한 설명, 중복 기록은 피한다.

## 결정 문서 규칙
- ADR은 5개까지 루트에 둘 수 있다.
- 6번째 ADR부터는 `docs/adr/`로 이동한다.
- 한 번 이동하면 이후 ADR도 `docs/adr/`에만 추가한다.
- 분리 시에는 `docs/adr/ADR-INDEX.md`를 함께 둔다.
- 문서 구조 변경은 새 결정으로 기록하고, 기존 규칙은 덮어쓰지 않는다.

## 작성 규칙
- 기본 언어는 한국어로 한다.
- 파일명, 명령어, 코드 식별자, 전문 용어는 필요할 때 원문 그대로 적는다.
- 항목은 짧고 명확하게 쓴다.
- 기존 기록은 수정하지 않고 새 턴으로 추가한다.

## 작업 원칙
- 가장 작은 올바른 변경을 우선한다.
- 기존 사용자의 변경 사항은 임의로 되돌리지 않는다.
- 불확실한 요구는 먼저 짧게 확인한다.
- 충돌하는 규칙이 있으면 사용자 요청을 우선한다.

## 검증 원칙
- 변경 후 가능한 범위에서 확인한다.
- 관련 테스트나 점검 명령이 있으면 우선 실행한다.
- 검증을 못 했으면 그 이유를 남긴다.

## 커뮤니케이션
- 진행 중인 작업은 짧게 공유한다.
- 결과는 변경점과 확인 결과 중심으로 전달한다.
- 불필요한 장황한 설명은 하지 않는다.

## 신규 프로젝트 초기화
- 필수 폴더가 있으면 생성 여부를 먼저 확인한다.
- 필요한 의존성이 있으면 설치 여부를 확인한다.
- 생성 또는 설치 후에는 기본 구조와 설치 상태를 검증한다.
- 구체적인 필수 폴더, 의존성, 검증 명령은 `PROJECT_SETUP.md`를 참조한다.
