# DECISIONS.md

## 목적
- 이 문서는 프로젝트의 중요한 결정을 `ADR` 형식으로 기록하는 템플릿이다.
- 결정의 결과뿐 아니라 배경, 이유, 영향도 함께 남긴다.

## 사용 기준
- 설계 방향, 운영 정책, 데이터 구조, 배포 방식처럼 되돌리기 어려운 선택을 기록한다.
- 단순한 작업 메모나 진행 상황은 `AGENT_BACKLOG.md`에 남긴다.
- 반복될 가능성이 있는 규칙은 `AGENTS.md`에 반영한다.
- ADR이 5개 이상이면 `docs/adr/`로 분리하고, `docs/adr/ADR-INDEX.md`를 함께 둔다.
- ADR 목록은 `docs/adr/ADR-INDEX.md`에서 확인한다.

## 참조 기록 규칙
- ADR 작성 시 중요한 판단 근거가 된 문서나 규칙을 함께 적는다.
- 참조 항목은 필요한 것만 간단히 적는다.
- 예: `AGENTS.md`, 하위 `AGENTS.md`, 관련 ADR, 파일 경로, 이슈 번호
- 참조가 결정에 직접 영향을 주지 않으면 생략할 수 있다.

## 작성 규칙
- 한 결정은 한 문서로 작성한다.
- 제목은 짧고 분명하게 쓴다.
- 본문에는 배경, 결정, 이유, 대안, 영향, 관련 항목을 포함한다.
- 결정이 바뀌면 기존 내용을 수정하기보다 새 ADR을 추가한다.

## 번호 규칙
- ADR 번호는 `ADR 0001` 형식으로 쓴다.
- 번호는 4자리 0 패딩을 사용한다.
- 새 ADR을 추가할 때마다 번호를 1씩 증가시킨다.
- 같은 번호를 재사용하지 않는다.

## 형식
```md
# ADR 0001: 결정 제목

- 날짜: 2026-07-26
- 상태: Proposed | Accepted | Deprecated | Superseded

## 배경
문제나 상황을 간단히 설명한다.

## 결정
무엇을 하기로 했는지 적는다.

## 이유
왜 이 결정을 했는지 적는다.

## 대안
검토한 다른 선택지를 적는다.

## 영향
이 결정으로 무엇이 달라지는지 적는다.

## 관련
- `file/path`
- 이슈 번호
- 참고 문서
- `docs/adr/ADR-INDEX.md`
```

## 예시
```md
# ADR 0001: 백로그와 결정 문서 분리

- 날짜: 2026-07-26
- 상태: Accepted

## 배경
작업 흐름과 중요한 의사결정이 한 문서에 섞이면 나중에 찾기 어렵다.

## 결정
작업 흐름은 `AGENT_BACKLOG.md`에 기록하고, 중요한 결정은 `DECISIONS.md`에 기록한다.

## 이유
- 기록 목적이 다르다.
- 작업 내역은 흐름 추적용이다.
- 결정 기록은 재검토와 근거 확인용이다.

## 대안
- 모든 내용을 `AGENTS.md`에만 기록한다.
- 모든 내용을 하나의 로그 파일에만 기록한다.

## 영향
- 의사결정 이력이 명확해진다.
- 나중에 같은 결정을 반복 검토하기 쉬워진다.
- 문서 수는 늘지만 유지보수성은 좋아진다.

## 관련
- `AGENT_BACKLOG.md`
- `AGENTS.md`
- `docs/adr/ADR-INDEX.md`
```

## 운영 원칙
- 기존 ADR을 덮어쓰지 않는다.
- 결정이 바뀌면 새 ADR을 추가해 이력을 남긴다.
- 가능한 한 날짜와 상태를 함께 기록한다.
